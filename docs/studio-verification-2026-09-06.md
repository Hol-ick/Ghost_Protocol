# Studio 기능·사용성 검증 — 2026-09-06

## 확인된 원인과 수정

동일한 `universe` 작업 3개를 이름만으로 표시하고 수정 시각 순으로 정렬했다. 설치된 Streamlit 선택 상자는 표시 문자열로 선택을 직렬화한다. 중복 이름과 순서 변경이 결합해 다른 작업이 선택되면서 분석·원고·비용이 초기화된 것처럼 보였다. 동일 이름 3개를 사용하는 전체 흐름 테스트가 수정 전 실패하고 수정 후 통과했다.

- 작업 선택을 생성 시각·고유 ID 순으로 고정하고 표시에도 ID를 넣었다. URL과 선택 상태를 동기화했다.
- 실제 준비 상태로 다음 단계를 복원하며 작업별 비용과 전체 누적 비용을 분리했다.
- 단계 탐색과 실행 영역을 분리해 한 단계만 표시한다. 상세 자료·원본·로그는 제한 높이 영역이나 대화상자에서 확인한다.
- 투명한 Streamlit 상단 툴바가 메뉴 클릭을 가로채는 것을 실제 DOM으로 확인하고 제거했다.
- 실행 로그와 조작 기록을 저장하고 진행 중 갱신한다. 실패는 새로고침 후에도 표시되며 예외 종류·메시지·호출 위치를 보존한다. 비밀값·URL 쿼리·로컬 변수는 오류 상세에서 제외한다.
- 초 단위 시각 충돌을 줄이기 위해 저장 시각 정밀도를 마이크로초로 변경했다. 격리 UI 검증 서버의 서비스 생성 인자 불일치도 수정했다.

## 검증 범위

| 기능 | 실행 근거 | 결과·경계 |
|---|---|---|
| 작업 생성·동일 이름 선택 | AppTest 전체 흐름, 실제 Chrome 선택 | 다른 작업으로 전환되지 않음; 선택 지우기도 현재 작업 유지 |
| 수집 정상·실패 | 격리 브라우저 및 자동 테스트 | 기존 수집기 연결 유지; 실패 후 분석 차단 |
| 분석·확인 | 격리 브라우저, AppTest | 분석 확인 후에만 원고 생성 가능 |
| 원고 생성 | 격리 3개 및 실제 API 1개 | 진행 로그·비용 저장, 검토 단계 전환 |
| 모델 비교 | 격리 브라우저 및 서비스 테스트 | 두 모델 결과와 실행 기록 확인 |
| 원고 수정·승인 | 격리 브라우저, 서비스 테스트 | 버전 저장, 승인 후 다음 원고; 오래된 버전 승인 거부 |
| 내보내기 | 서비스 테스트, 브라우저 버튼 확인 | Markdown/JSON 생성 검증; 브라우저 파일 저장 완료는 미검증 |
| 페르소나·규칙 | 브라우저 메뉴·규칙 저장, 자동 테스트 | 저장·복원 확인 |
| 설정 | 격리 브라우저, 서비스 테스트 | 생성 수 한도 저장·적용 확인 |
| 운영 기록·작업 로그 | 브라우저 및 자동 테스트 | 실행·API·조작·오류 상세 조회, 다운로드 내용 구성 확인 |
| 중단·중복 실행 | 자동 테스트 | 중복 실행 거부, 진행 중 중단 및 부분 결과 보존 |
| 새로고침·메뉴 이동·재시작 | AppTest, 서비스 재생성, 실제 브라우저 | 작업·원고·비용 복원 및 실패 기록 보존 |
| 기존 비교 자료 가져오기 | 기존 회귀 테스트 | 중복 가져오기 방지 |
| 화면 크기·클릭 | 1366×768, 1280×720, 390×844 실제 화면 | 데스크톱 저장·승인 버튼 화면 내 위치; 모바일 가로 넘침 없음 |
| 실제 게시판 재수집·로그인·게시 | 이번 변경에서는 실행 안 함 | 외부 접근 회복·게시 안정성 판단 제외 |
| 장시간 운영·전체 네트워크 추적 | 미실행 | 장기 안정성 및 전체 요청 waterfall 미검증 |

최종 `python -X utf8 -m pytest -q`: **422 passed, 2 skipped / 55.87초**. 두 skip은 실행 성공으로 계산하지 않는다.

실제 1280×720 검토 화면에서 저장·승인 버튼의 세로 위치는 612.36~648.75px로 페이지 스크롤 없이 접근 가능했다. 모바일 단계 메뉴는 3열 2행으로 조정했다. 최종 실제 Studio 브라우저 오류·경고 로그 조회는 빈 목록이었다. 화면 증거는 이번 대화의 브라우저 캡처에 있으며 별도 이미지 파일로 저장하지 않았다.

## 실제 API 확인

- 재사용 작업: `adff44d1a234446197a18404c2207fd2` (기존 수집·분석 결과 사용)
- 실행: `197ebef5b3b7491bb12abd82eebb9516`
- 모델: `gemini-3.1-flash-lite`, 페르소나: `cynical`, 원고 1개
- 전체 작업 2.723초, API 2.037초
- 입력 8,298 / 출력 102 / 합계 8,400 토큰
- 코드상 요금표 계산 비용 **$0.0022275** (공급자 청구서 대조 아님)
- 새로고침 후 원고·작업 비용 $0.00385·전체 누적 $0.01197 유지 확인

기존 작업 `2fd9a9fdd55e4d8db67090b8ea6eb80c`의 원고 3개와 비용 $0.00812는 삭제되지 않았다. 사용자의 Chrome Studio를 해당 작업의 검토 화면으로 다시 열어 원고 3개·검토 대기 3개를 확인했다. 이번 검증에서 실제 원고 승인이나 외부 게시를 수행하지 않았다.
